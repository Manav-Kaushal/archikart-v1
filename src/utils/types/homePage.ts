export type section2DataType = {
  title: string;
  description: string;
};

export type section3DataType = {
  title: string;
  img: string;
};

export type section4DataType = {
  title: string;
  description: string;
  img: string;
};
